function strCount(str, letter){  
  var count = 0; 
  var arr = str.split(''); 
  for (var i=0; i<arr.length; i++){
  if (arr[i]==letter) count ++; 
  }
  return count; 
}



OR: 

function strCount(str, letter){  
  return str.split(letter).length-1
}
 
